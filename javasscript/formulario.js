const username = document.getElementById('user').value;
const password = document.getElementById('senha').value;
const text = 'Erro!'
function Login()
{
    if (isNaN(username))
    {
       const message =  document.getElementById('mesage_error');
        message.innerText = text;
        a
    }
}